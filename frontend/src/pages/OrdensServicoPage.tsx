import{useCallback,useEffect,useMemo,useState}from'react';import{AlertCircle,Plus,SearchX}from'lucide-react';import{PageHeader}from'../components/PageHeader';import{OrderDetailsModal}from'../components/orders/OrderDetailsModal';import{OrdersFilters}from'../components/orders/OrdersFilters';import{OrdersList}from'../components/orders/OrdersList';import{OrdersLoading}from'../components/orders/OrdersLoading';import{getFleetPrefixes,getProjects,getServiceOrderDetails,getServiceOrders}from'../services/orders';import type{FleetPrefixOption,OrderFilters,ProjectOption,ServiceOrder,ServiceOrderDetails}from'../types/orders'
export function OrdensServicoPage(){const[draft,setDraft]=useState<OrderFilters>({}),[filters,setFilters]=useState<OrderFilters>({}),[orders,setOrders]=useState<ServiceOrder[]>([]),[projects,setProjects]=useState<ProjectOption[]>([]),[prefixes,setPrefixes]=useState<FleetPrefixOption[]>([]),[loading,setLoading]=useState(true),[optionsLoading,setOptionsLoading]=useState(true),[error,setError]=useState(false),[selected,setSelected]=useState<string|null>(null),[details,setDetails]=useState<ServiceOrderDetails|null>(null),[detailsLoading,setDetailsLoading]=useState(false),[detailsError,setDetailsError]=useState(false);const filtered=useMemo(()=>Object.values(filters).some(Boolean),[filters]);const load=useCallback(async(f:OrderFilters,signal?:AbortSignal)=>{setLoading(true);setError(false);try{setOrders(await getServiceOrders(f,signal))}catch{if(!signal?.aborted)setError(true)}finally{if(!signal?.aborted)setLoading(false)}},[]);useEffect(()=>{const c=new AbortController();void load(filters,c.signal);return()=>c.abort()},[filters,load]);useEffect(()=>{const c=new AbortController();Promise.all([getProjects(c.signal),getFleetPrefixes(c.signal)]).then(([a,b])=>{setProjects(a);setPrefixes(b)}).catch(()=>undefined).finally(()=>!c.signal.aborted&&setOptionsLoading(false));return()=>c.abort()},[]);const open=useCallback(async(id:string)=>{setSelected(id);setDetails(null);setDetailsLoading(true);setDetailsError(false);try{setDetails(await getServiceOrderDetails(id))}catch{setDetailsError(true)}finally{setDetailsLoading(false)}},[]);const clear=()=>{setDraft({});setFilters({})};return <><PageHeader title="Ordens de Serviço" subtitle="Consulte as ordens e acompanhe seus itens e valores." action={<button className="button button--primary" title="Criação disponível na próxima etapa"><Plus size={18}/>Nova O.S.</button>}/><OrdersFilters filters={draft} setFilters={setDraft} projects={projects} prefixes={prefixes} loadingOptions={optionsLoading} onSubmit={()=>setFilters({...draft})} onClear={clear}/><section aria-live="polite" className="orders-results"><header><h2>Ordens cadastradas</h2><p>{!loading&&!error?`${orders.length} registro(s) encontrado(s)`:'Consulta ao cadastro de ordens'}</p></header>{loading?<OrdersLoading/>:error?<Message error title="Não foi possível carregar as Ordens de Serviço." text="Verifique a conexão e tente novamente." action={<button className="button button--secondary" onClick={()=>void load(filters)}>Tentar novamente</button>}/>:orders.length?<OrdersList orders={orders} onView={id=>void open(id)}/>:<Message title={filtered?'Nenhuma Ordem de Serviço encontrada com os filtros informados.':'Nenhuma Ordem de Serviço cadastrada.'} text={filtered?'Revise os campos ou limpe os filtros.':'Quando houver ordens, elas aparecerão aqui.'} action={filtered?<button className="button button--secondary" onClick={clear}>Limpar filtros</button>:undefined}/>}</section>{selected&&<OrderDetailsModal order={details} loading={detailsLoading} error={detailsError} onClose={()=>setSelected(null)} onRetry={()=>void open(selected)}/>}</>}
function Message({error,title,text,action}:{error?:boolean;title:string;text:string;action?:React.ReactNode}){const Icon=error?AlertCircle:SearchX;return <div className={`orders-message${error?' orders-message--error':''}`}><Icon/><div><h3>{title}</h3><p>{text}</p></div>{action}</div>}
