import type{Project}from'../../types/projects';import type{ReportFilters}from'../../types/reports'
type Props={draft:ReportFilters;projects:Project[];error:string;onChange:(x:ReportFilters)=>void;onApply:()=>void;onClear:()=>void}
export function ReportsFilters({draft,projects,error,onChange,onApply,onClear}:Props){return <form className="reports-filters" onSubmit={e=>{e.preventDefault();onApply()}}><header><div><h2>Filtros do relatório</h2><p>Os filtros aplicados também serão usados nas exportações.</p></div></header><div className="reports-filters__grid"><label>Data inicial<input type="date" value={draft.data_inicio||''} aria-invalid={Boolean(error)} aria-describedby={error?'report-period-error':undefined} onChange={e=>onChange({...draft,data_inicio:e.target.value||undefined})}/></label><label>Data final<input type="date" value={draft.data_fim||''} aria-invalid={Boolean(error)} aria-describedby={error?'report-period-error':undefined} onChange={e=>onChange({...draft,data_fim:e.target.value||undefined})}/></label><label>Obra<select value={draft.obra_id||''} onChange={e=>onChange({...draft,obra_id:e.target.value||undefined})}><option value="">Todas as obras</option>{projects.map(x=><option key={x.id} value={x.id}>{x.codigo} — {x.nome}</option>)}</select></label></div>{error&&<p className="reports-filters__error" id="report-period-error" role="alert">{error}</p>}<footer><button className="button button--secondary" type="button" onClick={onClear}>Limpar filtros</button><button className="button button--primary">Aplicar filtros</button></footer></form>}