import{cloneElement,useEffect,useRef,useState,type ReactElement}from'react';import{LoaderCircle,X}from'lucide-react';import type{Project,ProjectPayload,ProjectStatus}from'../../types/projects'
type P={project:Project|null;saving:boolean;error:string|null;onClose:()=>void;onSave:(x:ProjectPayload)=>void};type Form={codigo:string;nome:string;descricao:string;status:ProjectStatus;data_inicio:string;data_fim:string;observacoes:string};type Errors=Partial<Record<keyof Form,string>>
const init=(x:Project|null):Form=>({codigo:x?.codigo||'',nome:x?.nome||'',descricao:x?.descricao||'',status:x?.status||'ATIVA',data_inicio:x?.data_inicio?.slice(0,10)||'',data_fim:x?.data_fim?.slice(0,10)||'',observacoes:x?.observacoes||''})
export function ProjectFormModal(p:P){const{saving,onClose}=p,[form,setForm]=useState(()=>init(p.project)),[errors,setErrors]=useState<Errors>({}),box=useRef<HTMLFormElement>(null);useEffect(()=>{const key=(e:KeyboardEvent)=>e.key==='Escape'&&!saving&&onClose();document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[saving,onClose]);const set=(k:keyof Form,v:string)=>{setForm(x=>({...x,[k]:v}));setErrors(x=>({...x,[k]:undefined}))};const submit=(e:React.FormEvent)=>{e.preventDefault();const x:Errors={};if(!form.codigo.trim())x.codigo='Informe o código da obra.';else if(form.codigo.trim().length>50)x.codigo='Use no máximo 50 caracteres.';if(!form.nome.trim())x.nome='Informe o nome da obra.';else if(form.nome.trim().length>255)x.nome='Use no máximo 255 caracteres.';if(form.data_inicio&&form.data_fim&&form.data_fim<form.data_inicio)x.data_fim='A data final não pode ser anterior à inicial.';setErrors(x);if(Object.keys(x).length){setTimeout(()=>box.current?.querySelector<HTMLElement>('[aria-invalid="true"]')?.focus());return}p.onSave({codigo:form.codigo.trim(),nome:form.nome.trim(),descricao:form.descricao||null,status:form.status,data_inicio:form.data_inicio||null,data_fim:form.data_fim||null,observacoes:form.observacoes||null})};return <div className="modal-backdrop"><form aria-labelledby="project-form-title" aria-modal="true" className="order-form project-form" onSubmit={submit} ref={box} role="dialog"><header><div><span>{p.project?'Editar cadastro':'Novo cadastro'}</span><h2 id="project-form-title">{p.project?'Editar obra':'Nova obra'}</h2></div><button aria-label="Fechar formulário" className="icon-button" disabled={saving} onClick={onClose} type="button"><X/></button></header><main><div className="form-grid"><Field k="codigo" t="Código" e={errors.codigo}><input autoFocus maxLength={50} value={form.codigo} onChange={e=>set('codigo',e.target.value)}/></Field><Field k="nome" t="Nome" e={errors.nome}><input maxLength={255} value={form.nome} onChange={e=>set('nome',e.target.value)}/></Field><Field k="status" t="Status"><select value={form.status} onChange={e=>set('status',e.target.value)}><option value="ATIVA">Ativa</option><option value="INATIVA">Inativa</option></select></Field><Field k="data_inicio" t="Data inicial"><input type="date" value={form.data_inicio} onChange={e=>set('data_inicio',e.target.value)}/></Field><Field k="data_fim" t="Data final" e={errors.data_fim}><input type="date" value={form.data_fim} onChange={e=>set('data_fim',e.target.value)}/></Field><label className="form-field form-field--wide">Descrição<textarea rows={3} value={form.descricao} onChange={e=>set('descricao',e.target.value)}/></label><label className="form-field form-field--wide">Observações<textarea rows={3} value={form.observacoes} onChange={e=>set('observacoes',e.target.value)}/></label></div>{p.error&&<p className="form-api-error" role="alert">{p.error}</p>}</main><footer><button className="button button--secondary" disabled={saving} onClick={onClose} type="button">Cancelar</button><button className="button button--primary" disabled={saving}>{saving?<><LoaderCircle className="spin" size={16}/>Salvando...</>:'Salvar obra'}</button></footer></form></div>}
function Field({k,t,e,children}:{k:keyof Form;t:string;e?:string;children:React.ReactElement}){const child=cloneElement(children as ReactElement<Record<string,unknown>>,{id:k,'aria-invalid':Boolean(e),'aria-describedby':e?`${k}-error`:undefined});return <label className="form-field" htmlFor={k}>{t}{child}{e&&<small id={`${k}-error`}>{e}</small>}</label>}
